# Pre-load configuration for zephyr completion plugin (loaded via antidote).
# Completion options, styles, and compinit are all handled by zephyr.
ZSH_COMPDUMP="${ZDOTDIR}/.zcompdump"
zstyle ':zephyr:plugin:completion' 'use-cache' yes
